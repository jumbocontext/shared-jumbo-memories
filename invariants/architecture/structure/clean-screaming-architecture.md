# Clean Screaming Architecture
Reasoning: Clear architectural intent and maintainability.

```bash
jumbo invariant add --title "Clean Screaming Architecture" --description "Layered boundaries (Domain -> Application -> Infrastructure -> Presentation). Inside each layer the naming of directories and files should SCREAM what the domain is and each pieces role in the domain" --enforcement "Code review"
```
