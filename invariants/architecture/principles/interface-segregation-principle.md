# Interface Segregation Principle
Reasoning: Smaller interfaces reduce accidental coupling and keep consumers dependent only on the behavior they actually need.

```bash
jumbo invariant add --title "Interface Segregation Principle" --description "Clients should not be forced to depend on interfaces they do not use." --enforcement "Code review"
```
