# Open-Closed Principle
Reasoning: Extension points let teams add behavior without repeatedly modifying stable code paths.

```bash
jumbo invariant add --title "Open-Closed Principle" --description "Software entities should be open for extension and closed for modification." --enforcement "Code review"
```
