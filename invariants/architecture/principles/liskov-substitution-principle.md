# Liskov Substitution Principle
Reasoning: Substitutability keeps inheritance and interface-based design from introducing surprising runtime behavior.

```bash
jumbo invariant add --title "Liskov Substitution Principle" --description "Derived types must be substitutable for their base types without breaking expected behavior." --enforcement "Code review"
```
