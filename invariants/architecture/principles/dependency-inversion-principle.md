# Dependency Inversion Principle
Reasoning: SOLID principle - flexibility and testability.

```bash
jumbo invariant add --title "Dependency Inversion Principle" --description "Depend on abstractions, not concretions. Application depends only on abstractions. Infrastructure implements those abstractions." --enforcement "Code review"
```
