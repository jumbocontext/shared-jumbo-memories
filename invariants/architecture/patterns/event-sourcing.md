# Event Sourcing
Reasoning: Provides an audit trail, replay capability, and temporal queries.

```bash
jumbo invariant add --title "Event Sourcing" --description "All state changes via events. Domain events live next to their entity" --enforcement "Code review"
```
