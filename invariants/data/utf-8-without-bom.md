# UTF-8 without BOM
Reasoning: Consistency and cross-platform compatibility.

```bash
jumbo invariant add --title "UTF-8 without BOM" --description "All new files must use UTF-8 encoding without Byte Order Mark"
```
